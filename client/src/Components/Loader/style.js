import {StyleSheet} from "aphrodite";

export default StyleSheet.create({
    container: {
        width: '100%',
        position: 'fixed',
        height: '100%',
        margin: 'auto',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
})